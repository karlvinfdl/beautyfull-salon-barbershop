# .gitlab-ci.yml pour Vite + GitLab Pages (SPA friendly)
image: node:18

stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

variables:
  NODE_ENV: production

before_script:
  - npm ci   # installe toutes les dépendances depuis package-lock.json

build:
  stage: build
  script:
    - npx vite build   # utilise le binaire Vite local, plus besoin de l'installer globalement
  artifacts:
    paths:
      - dist
    expire_in: 1 week

pages:
  stage: deploy
  script:
    # préparer le dossier public pour GitLab Pages
    - mkdir .public
    - cp -r dist/* .public/
    # créer un fallback 404.html pour SPA (routes côté client)
    - echo '<meta http-equiv="refresh" content="0; url=index.html">' > .public/404.html
  artifacts:
    paths:
      - .public
  only:
    - main
